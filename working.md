1. CRUD Posts
2. Each post has a "read more route"
  - Truncated `content` @ 200 characters
  - Has Read Post link `/post/:id`
      newPost.title
      newPost.content
      newPost.id
      newPost.kudosCount
      newPost.publishedAt